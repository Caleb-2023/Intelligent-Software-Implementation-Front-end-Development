<footer class="site-foot">
  <div class="site-foot__logo">
    <a href="/">
      <svg
        width="280"
        height="280"
        viewBox="0 0 280 280"
        fill="none"
        xmlns="http://www.w3.org/2000/svg"
      >
        <path
          d="M240.245 16.8V280h39.75V0h-160V280h39.755V16.8h19.8V280h39.755V16.8h19.8Z"
          fill="currentColor"
        >
        </path>
        <path d="M0 0v280h39.755V16.8H59.65V280h39.756V0H0Z" fill="currentColor">
        </path>
      </svg>

    </a>
  </div><!-- .site-foot__logo -->

  <button class="site-foot__feedback" type="button" data-am-open-feedback>
    Share Feedback
  </button>
</footer>

<style lang="scss">
  .site-foot {
    @include flex(row, center, center);

    position: relative;
    z-index: 3;

    padding: 1.5rem;

    border-top: 1px solid color(secondary);

    @include mq(phone) {
      padding: 1rem;
    }

    &__logo {
      a {
        color: color(secondary);
        text-decoration: none;
      }

      svg {
        display: block;
        width: 3rem;
        height: 3rem;
        color: color(secondary);

        @include mq(phone) {
          width: 1.875rem;
          height: 1.875rem;
        }
      }
    }

    &__feedback {
      position: absolute;
      right: 1.5rem;
      top: 50%;
      transform: translateY(-50%);

      padding: 0.5rem 1rem;

      border: 1px solid color(secondary);
      border-radius: 999px;
      background: transparent;
      color: color(secondary);

      font-family: font-family(fraktion);
      font-size: 0.7rem;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      cursor: pointer;

      transition: opacity 0.2s ease;

      &:hover {
        opacity: 0.6;
      }

      @include mq(phone) {
        right: 1rem;
        padding: 0.4rem 0.75rem;
        font-size: 0.6rem;
      }
    }
  }
</style>
